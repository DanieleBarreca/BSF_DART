import { SelectFieldFilterPipe } from './select-field-filter.pipe';

describe('SelectFieldFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SelectFieldFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
